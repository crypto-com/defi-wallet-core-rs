[workspace]
resolver = "2"
members = [
    "proto-build",
    "proto",
    "common",
    "bindings/wasm",
    "bindings/cpp"
]
default-members = [
    "proto-build",
    "proto",
    "common",
    "bindings/cpp"
]

[profile.release]
opt-level = "s"
debug = true
lto = "thin"
